# Assignment 5 Readme. 

The notebook is intended to be run from the `/content/` folder in Google Colab. 

`/content/Object_Tracking/` is the main dir for the dataset. The subdirs 

`MPEG-4` codec is used to generate `task1_input.mp4`, so make sure this format is compatible in your grading!

